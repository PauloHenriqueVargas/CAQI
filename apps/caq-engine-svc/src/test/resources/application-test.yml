spring:
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/seed   # testes precisam do golden dataset
  rabbitmq:
    listener:
      simple:
        auto-startup: false

caqi:
  events:
    enabled: false   # testes não precisam de RabbitMQ rodando
  security:
    users:
      leitor:
        username: leitor_test
        password: leitor_test_password
      gestor:
        username: gestor_test
        password: gestor_test_password
      admin:
        username: admin_test
        password: admin_test_password
